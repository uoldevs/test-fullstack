{
  "name": "back-end",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "node .",
    "db:reset": "rm db.sqlite && npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "prestart": "npm run build && npm run db:reset",
    "predev": "npm run build && npm run db:reset",
    "dev": "nodemon --watch \"src/**\" --ext \"ts,json,js\" --exec \"ts-node src/api/server.ts\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "sequelize": "^6.32.1",
    "sqlite3": "^5.1.6",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "@types/validator": "^13.11.1",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0"
  }
}
